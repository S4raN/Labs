#include <stdio.h>
#include <math.h>

int main() {
    float x, y;
    printf("Введите значение x:\n");
    scanf("%f", &x);
    if (x < 4) { 
       y = powf(x, 5) - powf(x, 2);
    }
    else if(x >= 4) {
        y = powf(x, 3);
    }
    printf("Результат: y = %.2f\n", y);
    return 0;
}
