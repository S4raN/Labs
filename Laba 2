#include <stdio.h>
#include <math.h>

int main() {
    float x, y;
    printf("Введите значение x:\n");
    scanf("%f", &x);
    if (x < 4) { 
       y = powf(x, 5) - powf(x, 2);
    }
    else {
        y = powf(x, 3);
    }
    printf("Результат: y = %f\n", y);
    return 0;
}
