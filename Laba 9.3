```c
#include <stdio.h>
#include <string.h>

int containsTwoDigitNumber(char *string) {
    int i = 0;
    while (string[i] != '\0') {
        if (isdigit(string[i]) && isdigit(string[i+1])) {
            return 1;
        }
        i++;
    }
    return 0;
}

int main() {
    char input[100];
    while (fgets(input, sizeof(input), stdin)) {
        if (!containsTwoDigitNumber(input)) {
            printf("%s", input);
        }
    }
    return 0;
}
```

Данный код использует функцию `containsTwoDigitNumber`, которая проверяет наличие двузначного числа в заданной строке. Затем основная функция `main` использует `fgets` для чтения строк из стандартного ввода (например, из консоли) и выводит только те строки, которые не содержат двузначные числа.
